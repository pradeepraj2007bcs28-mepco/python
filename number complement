#include <stdio.h>
int findComplement(int num) {
    int todo = num;
    int mask = 0;
    while (todo > 0) {
        mask = (mask << 1) | 1; 
        todo = todo >> 1;       
    }
    return num ^ mask;
}
